var fs = require('fs');

var contents;

try {
  contents = fs.readFileSync('non-existant').toString();
}
catch(error){
  if(error.code !== 'EOENT') {
    console.log('file doesn\'t exist. please run blah');
  }
}

if(!contents){
  console.log('lol');
}
